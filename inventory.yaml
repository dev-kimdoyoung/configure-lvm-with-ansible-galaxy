all:
    hosts:
        my-server:
            ansible_host: "your host ip"
            ansible_port: 22
            ansible_user: admin
            ansible_password: "your passsword"
            ansible_connection: ssh
